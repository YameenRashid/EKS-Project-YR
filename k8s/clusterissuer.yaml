apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt-prod          # renamed
spec:
  acme:
    email: yameenr7@gmail.com
    server: https://acme-v02.api.letsencrypt.org/directory   # changed from acme-staging-v02 to the real production URL
    privateKeySecretRef:
      name: letsencrypt-prod-key   # also rename this, keep it consistent
    solvers:
    - http01:
        ingress:
          ingressClassName: nginx